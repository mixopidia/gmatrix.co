# App
PORT=4000
NODE_ENV=development

# Database
MONGO_URI=mongodb://127.0.0.1:27017/mixopidia

# Auth
JWT_SECRET=your_jwt_secret_here
JWT_EXP=7d

# Chain / RPC (BSC single-chain)
CHAIN=BSC
RPC_URL=https://bsc-dataseed.binance.org/
CONFIRMATIONS=12

# Hot wallet secrets (we’ll fill later)
# Consider encrypting and storing elsewhere; for now keep blank
HOT_WALLET_ADDRESS=
HOT_WALLET_PRIVATE_KEY=
HOT_ENC_PATH=./secrets/hot.enc

# Cold storage (Safe / multisig)
COLD_SAFE_ADDRESS=0xCOLD_SAFE_MULTISIG_ADDRESS
SWEEP_TO_ADDRESS=0xYOUR_COLLECTOR_ADDRESS

# Markets / assets (placeholders; we’ll refine later)
DEFAULT_ASSETS=USDT,BNB,BTCB,ETH
USDT_CONTRACT=0x...
BTCB_CONTRACT=0x...
ETH_CONTRACT=0x...

# Market data (Binance)
BINANCE_REST=https://api.binance.com
BINANCE_WS=wss://stream.binance.com:9443

# CORS (set to your frontend URL later)
CORS_ORIGIN=http://localhost:5173

# Optional rate limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=300

# Admin bootstrap
ADMIN_EMAIL=admin@mixopidia.com
ADMIN_PASSWORD=ChangeMe_Strong!
